*.pyc
__pycache__/
state/
.installed
dist/
.claude/

# downloaded python-obd source (already baked into the venv; not shipped)
obd-0.7.3/

# user-supplied game ROMs -- never commit these (legal); keep only the readme
roms/*
!roms/README.txt
