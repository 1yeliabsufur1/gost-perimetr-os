# Installed to /etc/bluetooth/main.conf by install.sh (Part 1). Powers the
# controller at boot so the OBD rfcomm link script doesn't have to, and
# shortens page-scan reconnect latency after ignition-on.
[General]
FastConnectable=true

[Policy]
AutoEnable=true
